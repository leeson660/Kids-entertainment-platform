// Tailwind CSS v4 — theme configuration lives in app/globals.css (@theme blocks)
// This file is kept for IDE autocomplete and any v4-compatible plugin setup.
import type { Config } from "tailwindcss"

const config: Config = {
  // Content auto-detected in Tailwind v4 — no need to specify
  content: [],
}

export default config
